<html>
<head>
   <meta charset="UTF-8" />
    <title>EJS Example</title>
</head>
<body>
    <h1>Favorite Movies</h1>

    <ul>
        <% data.forEach(function(item){ %>
        <li> <a href='<%= item.movieId %>'>  <%= item.title %> </a></li>
        <% }); %>
    </ul>

</body>
</html>
