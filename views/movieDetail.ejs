<html>

<head>
    <meta charset="UTF-8" />
    <title>영화 상세 정보</title>
</head>

<body>
    <h1>영화 상세 정보</h1>
    <div>
        <ul>
            <li><h4> 제목 :  <%= movie.title %></h4></li>
            <li><h4> 감독 :  <%= movie.director %></h4></li>
            <li><h4> 연도 :  <%= movie.year %></h4></li>
            <li><h4> 요약 줄거리 :  <%= movie.synopsis %></h4></li>
        </ul>
    </div>
    <div>
      <h3>리뷰( <%= movie.reviews.length %> )</h3>
      <ul>
          <% for (var i=0; i<movie.reviews.length; i++){ %>
            <% var review = movie.reviews[i] %>
            <li><%= review %></li>
          <% } %>
      </ul>
      <form method="post" action="/movies/<%= movie.movieId %>">
        <input type='text' name='review' placeholder='내용을 입력하세요' />
        <input type='submit' value="입력"/>
      </form>
    </div>
</body>
</html>
